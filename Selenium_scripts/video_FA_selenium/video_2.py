from selenium import webdriver

driver = webdriver.Chrome()

driver.get("https://www.techwithtim.net")
print(driver.title)

#first attempt at looking through element variables 
search = ""

driver.find_element

driver.quit()
